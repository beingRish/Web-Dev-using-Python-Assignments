/*6. Declare a function name capitalizeArray. It takes array as a parameter and it returns
the - capitalizedarray.
*/

let capitalizeArray = (a, b, c, d, e) => {
    
};

arrayToPass = ['Rishabh', 'Singh', 'a', 'hardworking', 'person'];
let returnedArray = capitalizeArray(arrayToPass);
console.log(returnedArray);
























